"use strict";
//Making an array of users:
let userName = ["admin", "Fatima", "aisha", "Hina", "Aneela"];
if (userName.length === 0) {
    console.log("We need to find some users.");
}
else {
    userName = [];
    console.log(" All users name have been removed. " + userName.length);
}
